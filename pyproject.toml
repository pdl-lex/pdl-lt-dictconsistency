[project]
name = "pdl-ltlabtools-dictconsistency"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128.0",
    "lxml>=6.0.2",
    "python-multipart>=0.0.18",
    "uvicorn[standard]>=0.49.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",  # für starlette TestClient in den API-Tests
]
